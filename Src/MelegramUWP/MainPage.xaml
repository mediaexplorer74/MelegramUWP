<Page
    x:Class="MelegramUWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MelegramUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Background="Transparent">
        <WebView x:Name="Browser"
                 HorizontalAlignment="Stretch"
                 VerticalAlignment="Stretch"
                 NavigationFailed="Browser_NavigationFailed" />
        <CommandBar VerticalAlignment="Bottom">
            <AppBarButton Icon="Back" Label="Back" Click="BackApplicationBar_Click" />
            <AppBarButton Icon="Setting" Label="Settings" Click="ForwardApplicationBar_Click" />
            <AppBarSeparator />
            <AppBarButton Icon="Home" Label="Home" Click="HomeMenuItem_Click" />
            <AppBarButton Icon="Contact" Label="Contacts" Click="Contacts_Click" />
            <AppBarButton Icon="Find" Label="Search" Click="Search_Click" />
            <AppBarButton Icon="ClosePane" Label="Sign out" Click="sg_Click" />
            <AppBarButton Icon="Help" Label="About" Click="ab_Click" />
        </CommandBar>
    </Grid>
</Page>
